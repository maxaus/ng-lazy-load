div.header
    h3.text-muted Lazy loaded list

div.jumbotron
    h2 GitHub user events
    h4 Try infinite scroll
    ul(lazy-load, lazy-data="newsList", lazy-data-provider="NewsFeedService", lazy-load-method="getNewsList", lazy-range="10").news-list
        //- we should use class="soon" or class=="in-progress" or class="completed" in the line below
        li(ng-repeat="event in newsList track by $index")
            .thumbnail-container
                img(ng-src="{{event.actor.avatar_url}}").avatar
                br
                a(href="{{event.actor.url}}", target="_blank") {{event.actor.login}}
            .info-container
                | Repo:
                a(href="{{event.repo.url}}", target="_blank") {{event.repo.name}}
                br
                | Change time: {{event.created_at | date:'dd.MM.yyyy HH:mm'}}
            .cleared
div.footer
    p